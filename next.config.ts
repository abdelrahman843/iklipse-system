import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev-tools overlay (Route / Bundler / Preferences) that
  // appears bottom-left in development.
  devIndicators: false,
};

export default nextConfig;
